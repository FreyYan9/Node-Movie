extends ../layout

block content
	.container
		.row
			form.form-horizontal(method="post",action='/admin/Movie_category/save')
				//- input(type="hidden",name="movie[_id]",value="#{movie._id}")
				.form-group
					lable.col-sm-2.control-label(for="category") 电影分类
					.col-sm-10
						input#category.form-control(type="text",name="category[name]",value="#{category.name}")

				.form-group
					button.col-sm-4.col-sm-offset-4.btn.btn-defualt 提交

							

